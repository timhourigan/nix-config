# Workflow to build Home Manager configuration from the flake
name: build-home-manager
run-name: Build Home Manager
on:
  push:
  pull_request:
jobs:
  build-hm:
    strategy:
      matrix:
        user: [timh]
        host: [x13, x1c]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      # https://github.com/cachix/install-nix-action
      - name: Install Nix
        uses: cachix/install-nix-action@v26
        with:
          nix_path: nixpkgs=channel:nixos-23.11
      - name: Build
      # Only doing `dry-run` as a full build has begun to
      # hang at `hm_fontconfigconf.d10hmfonts.conf.drv`
        run: |
          nix-shell -p home-manager gnumake --run "make build-hm-dry-run"
        env:
          USER: ${{ matrix.user }}
          HOSTNAME: ${{ matrix.host }}
        timeout-minutes: 15
